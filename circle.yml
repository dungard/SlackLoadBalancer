machine:
  services:
    - docker
  java:
    version: openjdk8  

environment:
  RABBIT_MASTER: localhost  
    
dependencies:
  pre:
    - docker pull harbur/rabbitmq-cluster:latest
    
test:    
    pre:
      - docker swarm init
    
    override:
      - chmod +x dockersetup.sh
      - dockersetup.sh